<section class="hero" aria-label="Do iOS 2026">
  <div class="hero-glow" aria-hidden="true"></div>
  <div class="hero-inner">
    <p class="hero-eyebrow">Amsterdam · NEMO Science Museum</p>
    <h1>Do iOS <span class="year">2026</span></h1>
    <p class="hero-tagline">iOS Developer Conference</p>
    <p class="hero-intro">
      Three days of workshops, talks, and networking with Europe's iOS community.
    </p>

    <div class="hero-dates" role="list" aria-label="Conference dates">
      <div class="date-pill" role="listitem">
        <span class="date-label">Workshops</span>
        <span class="date-value">Nov 10</span>
      </div>
      <div class="date-pill date-pill--primary" role="listitem">
        <span class="date-label">Conference</span>
        <span class="date-value">Nov 11–12</span>
      </div>
    </div>

    <div class="hero-actions">
      <a class="btn btn-primary" href="#tickets">Get tickets</a>
      <a class="btn btn-secondary" href="#call-for-papers">Submit a talk</a>
    </div>
  </div>

  <div class="schedule-strip">
    <article class="schedule-day">
      <time datetime="2026-11-10">Mon, Nov 10</time>
      <strong>Workshop day</strong>
      <span>In-depth hands-on sessions</span>
    </article>
    <article class="schedule-day schedule-day--highlight">
      <time datetime="2026-11-11">Tue, Nov 11</time>
      <strong>Conference day 1</strong>
      <span>Talks, lunch &amp; walking dinner</span>
    </article>
    <article class="schedule-day">
      <time datetime="2026-11-12">Wed, Nov 12</time>
      <strong>Conference day 2</strong>
      <span>More talks &amp; community</span>
    </article>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    color: white;
    background: var(--hero-bg);
    border-radius: var(--radius-xl);
    margin-bottom: 1.5rem;
    box-shadow: var(--shadow-soft);
  }

  .hero-glow {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 15% 40%, var(--hero-glow-1), transparent 42%),
      radial-gradient(circle at 85% 15%, var(--hero-glow-2), transparent 38%),
      radial-gradient(circle at 70% 90%, var(--hero-glow-3), transparent 35%);
    pointer-events: none;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 3.5rem 1.5rem 2.5rem;
    max-width: 720px;
    margin: 0 auto;
  }

  .hero-eyebrow {
    display: inline-block;
    margin: 0 0 1.25rem;
    padding: 0.35rem 0.85rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.12);
    border: 1px solid rgba(255, 255, 255, 0.18);
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  h1 {
    font-size: clamp(3rem, 10vw, 4.75rem);
    line-height: 1;
    letter-spacing: -0.04em;
    font-weight: 800;
    margin: 0 0 0.5rem;
  }

  .year {
    background: linear-gradient(120deg, #ff6b6b, #feca57, #48dbfb, #a29bfe);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-tagline {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 500;
    margin: 0 0 1rem;
    opacity: 0.92;
  }

  .hero-intro {
    font-size: 1.125rem;
    line-height: 1.65;
    margin: 0 auto 1.75rem;
    max-width: 34rem;
    color: rgba(255, 255, 255, 0.82);
  }

  .hero-dates {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  .date-pill {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    padding: 0.75rem 1.25rem;
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.14);
    min-width: 8.5rem;
  }

  .date-pill--primary {
    background: rgba(255, 255, 255, 0.16);
    border-color: rgba(255, 255, 255, 0.28);
  }

  .date-label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    opacity: 0.75;
  }

  .date-value {
    font-size: 1.125rem;
    font-weight: 700;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.75rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 1.6rem;
    border-radius: 999px;
    font-size: 1rem;
    font-weight: 700;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .btn-primary {
    background: white;
    color: #0a2463;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.18);
  }

  .btn-primary:hover {
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.22);
  }

  .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.45);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.7);
  }

  .schedule-strip {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: rgba(255, 255, 255, 0.12);
    border-top: 1px solid rgba(255, 255, 255, 0.12);
  }

  .schedule-day {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1.25rem 1rem;
    background: rgba(0, 0, 0, 0.18);
    text-align: center;
  }

  .schedule-day--highlight {
    background: rgba(255, 255, 255, 0.08);
  }

  .schedule-day time {
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    opacity: 0.7;
  }

  .schedule-day strong {
    font-size: 1rem;
    font-weight: 700;
  }

  .schedule-day span {
    font-size: 0.875rem;
    line-height: 1.4;
    opacity: 0.75;
  }

  @media (max-width: 768px) {
    .hero-inner {
      padding: 2.5rem 1rem 2rem;
    }

    .schedule-strip {
      grid-template-columns: 1fr;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      width: 100%;
    }
  }
</style>
